@using TaskManager.Domain.Utils
@using TaskManager.View.Utils
@{
    ViewData["Title"] = "Документ не найден";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <div class="card shadow">
                <div class="card-body p-4">
                    <!-- Иконка и заголовок -->
                    <div class="mb-4">
                        <div class="mb-3">
                            <i class="fas fa-file-excel fa-4x text-warning"></i>
                        </div>
                        <h3 class="fw-bold text-dark mb-2">
                            Документ не найден
                        </h3>
                        <p class="text-muted mb-4">
                            Запрашиваемый документ больше не существует в системе
                        </p>
                    </div>

                    <!-- Описание проблемы -->
                    <div class="alert alert-warning mb-4">
                        <div class="d-flex align-items-start">
                            <i class="fas fa-exclamation-circle me-2 mt-1"></i>
                            <div>
                                <p class="mb-0">
                                    Возможно, документ был удалён или у вас нет доступа для его просмотра.
                                </p>
                            </div>
                        </div>
                    </div>

                    <!-- Кнопки действий -->
                    <div class="d-flex flex-column flex-md-row gap-2 justify-content-center mb-4">
                        <a asp-controller="Documents" asp-action="Index"
                           class="btn btn-primary px-4">
                            <i class="fas fa-list me-2"></i>
                            К документам
                        </a>

                        <a asp-controller="Documents" asp-action="Create"
                           class="btn btn-outline-success px-4">
                            <i class="fas fa-plus me-2"></i>
                            Создать новый
                        </a>
                    </div>

                    <!-- Дополнительная информация -->
                    <div class="mt-4 pt-3 border-top">
                        <p class="text-muted small mb-2">
                            <i class="fas fa-info-circle me-1"></i>
                            Если документ должен существовать, обратитесь к администратору
                        </p>
                        <p class="text-muted small">
                            <i class="far fa-clock me-1"></i>
                            @DateTime.Now.ToString(DateFormatDictionary.DateTimeFormatDdMmYyyyHhMm)
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>